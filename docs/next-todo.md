# Next TODO

- [ ] Magic link doesn't work well with iOS home screen PWA — it opens Safari instead of staying in the app. Need a UI-based auth alternative (e.g., paste-a-code flow, or OTP input) for PWA context.
- [ ] Improve the magic link email experience — title, branding, UX of the email itself. Currently uses Supabase defaults which are generic.
